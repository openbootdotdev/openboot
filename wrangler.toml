name = "openboot"
main = "worker/index.js"
compatibility_date = "2024-01-01"

# Static assets configuration
[assets]
directory = "./website"

# Custom domain - configured via wrangler.toml
routes = [
  { pattern = "openboot.dev", custom_domain = true }
]
